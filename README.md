# ExcelWriter
First Step:
  >> pip install re
  >> pip install pandas
  >> pip install glob
  >> pip install tabulate

Second Step:
  Edit your directory name
  e.g. directory = '../MY_DIRECTORY/'
 
Thrid Step:
  run python script
  >> python ExcelWriter.py
